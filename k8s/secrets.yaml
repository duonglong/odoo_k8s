# ⚠️ For production, use Sealed Secrets, SOPS, or a secrets manager (Vault).
# This file is for development / demo purposes only.
# Generate base64 values: echo -n 'value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: odoo-secrets
  namespace: odoo
  labels:
    app.kubernetes.io/name: odoo
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # echo -n 'odoo' | base64
  db-user: b2Rvbw==
  # echo -n 'odoo' | base64
  db-password: MTIzNDU2
  # echo -n 'admin' | base64
  admin-password: YWRtaW4=
